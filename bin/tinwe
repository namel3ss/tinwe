#!/usr/bin/env ruby

# frozen_string_literal: true

require_relative '../lib/tinwe'
require 'byebug'

begin
  $stdout.puts Tinwe::Command.new(ARGV).execute
rescue Tinwe::CommandError => e
  warn e.message
  exit(1)
end
