#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative '../lib/tinwe'
require 'byebug'

begin
  $stdout.puts Tinwe::Command.execute(ARGV)
rescue CommandError => e
  warn e.message
  exit(1)
end
